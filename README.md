# alcero-app

Minimal NestJS scaffold. App listens on port 3000 and exposes a default GET / route returning "Hello World!".

Quick start (PowerShell):

```powershell
npm install
npm run start:dev
```

Or build and run production:

```powershell
npm run build
npm start
```
